# =============================================================================
# POC GeoServer + SQL Server — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit the actual .env file.
# =============================================================================

# -----------------------------------------------------------------------------
# SQL Server 2022
# -----------------------------------------------------------------------------
SA_PASSWORD=YourStrong!Password123
SQLSERVER_HOST=sqlserver
SQLSERVER_PORT=1433
SQLSERVER_DB=geo_poc
SQLSERVER_USER=sa

# -----------------------------------------------------------------------------
# PostgreSQL 16 / PostGIS 3.4
# -----------------------------------------------------------------------------
POSTGRES_HOST=postgis
POSTGRES_PORT=5432
POSTGRES_DB=geo_poc
POSTGRES_USER=geouser
POSTGRES_PASSWORD=geopassword

# -----------------------------------------------------------------------------
# GeoServer 2.25+
# -----------------------------------------------------------------------------
GEOSERVER_HOST=geoserver
GEOSERVER_PORT=8080
GEOSERVER_USER=admin
GEOSERVER_PASSWORD=geoserver
GEOSERVER_WORKSPACE=poc-geoserver

# -----------------------------------------------------------------------------
# API (Node.js / Fastify)
# -----------------------------------------------------------------------------
API_PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# AI Layer (Anthropic / Claude API)
# Obtain your key at: https://console.anthropic.com
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-replace-with-your-key
AI_MODEL=claude-sonnet-4-6
AI_MAX_TOKENS=2048

# -----------------------------------------------------------------------------
# Benchmark Runner
# -----------------------------------------------------------------------------
BENCHMARK_DURATION=30s
BENCHMARK_VUS=10
BENCHMARK_WARMUP=5s

# -----------------------------------------------------------------------------
# Seed Generator
# Fixed seed ensures reproducible synthetic data across environments.
# Change only if you need a different dataset layout.
# -----------------------------------------------------------------------------
SEED_RANDOM_SEED=42
SEED_POINTS_COUNT=50000
SEED_LINES_COUNT=100000
SEED_POLYGONS_COUNT=2000
SEED_INCONSISTENCIES_COUNT=15000
